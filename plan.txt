Flow Diagram :-
Incoming Request
        ↓
WebFlux Filter Chain
(Spring Security Filters)
        ↓
DispatcherHandler
        ↓
(DispatcherHandler will  Check)
        ↓
 ┌──────────────────────────────┐
 │ Local Controller Found ?     │
 └──────────────────────────────┘
        ↓ Yes                           ↓ No
 Execute Controller             Check Gateway Route Mapping
                                         ↓
                           ┌────────────────────────┐
                           │ Route Found ?          │
                           └────────────────────────┘
                                   ↓ Yes                 ↓ No
                           Execute Gateway Filters       404
                           (JWT / RateLimiter)
                                   ↓
                           Forward to Microservice
                                   ↓
                               Response

overview :
1: auth service (email , password ) : register , login 
2: bug service :
    createBug , getAllBugs , getBugById , updateBug(by id) , deletebug(by id) 
3. user managment 
   getAllUsers , getUserById , getUserByEmail , updateUser(only by id) , deleteUser(only by id)



//pending
// learn spring cloud , webflux and spring security
// remove the adding of  co-relation adding in globalException (but not from loggin) api gateway will do that
// make the execption and use it as library 


